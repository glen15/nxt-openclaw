# 00. 핸즈온 제작 지시문 (사내용)

이 문서는 OpenClaw 핸즈온 콘텐츠를 제작할 때 따라야 하는 **필수 기준**을 정의한다.

---

## 1. 필수 운영 원칙

아래 항목은 모두 **필수**다.

1. AWS 인프라는 **Terraform이 아닌 AWS Console**로 생성한다.
2. EC2는 각자 생성한 **개인 키 페어(.pem)** 로 SSH 접속한다.
3. AI 인증은 결제 API 키 대신 **개인 Google 계정 기반 무료 Auth 경로**를 사용한다.
4. Slack 연동은 공용 워크스페이스가 아닌 **각자 본인 Slack 워크스페이스**에서 앱/봇을 만든다.

## 2. 표준 실습 시나리오

### 2-1. AWS Console에서 EC2 생성

- 리전: `ap-northeast-2` (서울)
- AMI: `Ubuntu Server 22.04 LTS`
- 인스턴스: `t3.small`
- 스토리지: `30GB gp3`
- 보안그룹:
  - `22` (SSH): 본인 공인 IP만 허용
  - `18789` (Gateway): 본인 공인 IP만 허용
  - `443` (선택): 필요한 경우에만 본인 공인 IP 허용

### 2-2. SSH 키 접속

로컬 PC에서:

```bash
chmod 400 ~/.ssh/<내-키페어>.pem
ssh -i ~/.ssh/<내-키페어>.pem ubuntu@<EC2_PUBLIC_IP>
```

### 2-3. OpenClaw 설치 및 온보딩 시작

EC2 서버에서:

```bash
curl -fsSL https://openclaw.ai/install.sh | bash
openclaw onboard
```

채널은 `Slack (Socket Mode)`를 선택한다.

### 2-4. 개인 Slack 워크스페이스에서 앱/봇 생성

- `https://api.slack.com/apps`에서 본인 워크스페이스 선택
- 앱 생성 후 `Bot Token (xoxb-...)`, `App-Level Token (xapp-...)` 발급
- 온보딩 입력 단계에 각 토큰을 입력

### 2-5. 개인 Google 계정 기반 무료 Auth 인증

EC2 서버에서:

```bash
claude setup-token
```

- 브라우저 인증 단계에서 **개인 Google 계정**으로 로그인
- 발급된 OAuth 토큰을 OpenClaw에 설정

```bash
openclaw config set env.vars.ANTHROPIC_API_KEY "<발급된-토큰>"
```

### 2-6. 동작 확인

- Slack 채널/DM에서 봇 호출 후 응답 확인
- 대시보드 접속: `http://localhost:18789/?token=<GATEWAY_TOKEN>`
- 점검 명령:

```bash
openclaw doctor
openclaw gateway status
openclaw config get gateway.auth.token
```

---

## 3. 제출물 요구사항 (직원별)

각 직원은 아래를 제출한다.

1. 실습 문서 1부 (Markdown)
2. 단계별 스크린샷 (민감정보 마스킹)
3. 실패/해결 사례 최소 1건
4. 최종 검증 로그:
   - SSH 접속 성공
   - Slack 메시지 응답 성공
   - Dashboard 접속 성공

---

## 4. 작성 가이드

- 기존 문서 중 상세 절차는 아래를 참고하되, 실습 본문은 반드시 콘솔 수동 기준으로 재작성한다.
  - `docs/walkthrough/02-openclaw-설치.md`
  - `docs/walkthrough/03-대시보드-가이드.md`
  - `docs/walkthrough/04-운영-레퍼런스.md`
- 기존 `docs/walkthrough/01-ec2-생성.md`는 Terraform 기반이므로 **이번 과제의 실행 기준이 아니다**.
